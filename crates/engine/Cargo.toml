[package]
name = "trading-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
crc32c = "0.6"
libc = { workspace = true }
trading-disruptor = { path = "../disruptor" }

[dev-dependencies]
tempfile = "3"
