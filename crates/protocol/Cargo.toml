[package]
name = "trading-protocol"
version = "0.1.0"
edition = "2024"

[features]
latency-trace = ["trading-engine/latency-trace"]

[dependencies]
base64 = { workspace = true }
ed25519-dalek = { workspace = true }
trading-engine = { path = "../engine" }

[dev-dependencies]
tempfile = "3"
