[package]
name = "melin-app"
version = "0.8.0"
edition = "2024"

[lib]
name = "melin_app"
path = "src/lib.rs"

[dependencies]
base64 = { workspace = true }
libc = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tempfile = "3"
