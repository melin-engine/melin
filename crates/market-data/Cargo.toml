[package]
name = "melin-market-data"
version = "0.1.0"
edition = "2024"

[dependencies]
melin-engine = { path = "../engine" }
melin-protocol = { path = "../protocol" }
tracing = { workspace = true }

[dev-dependencies]
proptest = "1"
